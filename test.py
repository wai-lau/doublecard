from board_synth import BoardSynth

width, height = 8, 12;
board = [['' for _ in range(height)] for _ in range(width)]
# coordinates are board[column][row]
# bottom left block is (A,0)

board_synth = BoardSynth()

import ipdb; ipdb.set_trace()
